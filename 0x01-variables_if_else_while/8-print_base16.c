#include <stdio.h>
/**
 * main - Entry point
 * Description: prints base 16 numbers
 * Return: 0.
 */
int main(void)
{
char abd;
for (abc = '0'; abc <= '9'; abc++)
putchar(abc);
for (abc = 'a'; abc <= 'f'; abc++)
putchar(abc);
putchar('\n');
return (0);
}
